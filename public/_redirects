# Netlify _redirects file for SPA (Single Page Application)
# This ensures all routes are handled by React Router

# Handle SPA routing - serve index.html for all routes
/*    /index.html   200

# Optional: Specific redirects for common variations (if needed)
# /wifi-qr-code-generator    /wifi-qr-generator    301
# /vcard-qr-code-generator   /business-card-qr-generator    301

# Force HTTPS (optional - Netlify can do this in dashboard too)
# http://qr-pro-generator.com/*    https://qr-pro-generator.com/:splat    301!
# http://www.qr-pro-generator.com/*    https://qr-pro-generator.com/:splat    301!
